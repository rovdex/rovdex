#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
Rovdex does not currently publish a script-installable CLI archive.

Use release assets directly instead:
- macOS: download the Rovdex-macOS-*.dmg package
- Windows: download the Rovdex-Windows-*.exe installer
EOF

exit 1